export const envs = <%- JSON.stringify((('undefined' === typeof envs) ? {} : envs), null, 4) %>;

export const defaultEnv = <%- JSON.stringify(('undefined' === typeof defaultEnv) ? 'prod' : defaultEnv) %>;